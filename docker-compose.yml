services:
  db:
    image: postgres:16-alpine
    container_name: app-sci-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 21Déc1977!
      POSTGRES_DB: sci
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
